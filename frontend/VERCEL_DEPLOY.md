# Vercel — Frontend (feuji-gentera)
#
# Project settings:
#   Root Directory: frontend
#   Framework: Vite
#   Build Command: npm run build
#   Output Directory: dist
#
# Environment Variables (Production + Preview):
#   VITE_API_BASE_URL=https://feuji-gentera.onrender.com
#   VITE_WS_BASE_URL=wss://feuji-gentera.onrender.com
#
# On Render, allow the Vercel origin:
#   CORS_ORIGINS=https://feuji-gentera.vercel.app
#
# After changing VITE_* vars, Redeploy (they are baked in at build time).
